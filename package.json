{
  "name": "apilab-app",
  "version": "1.0.0",
  "description": "API Lab is a app that provides a design tool for the API First development approach, from Contract First to Sandbox Try-on",
  "author": "aplicalab.com",
  "license": "AGPL-3.0",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "electron": "^31.4.0"
  }
}
